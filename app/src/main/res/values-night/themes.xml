<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Projecth1" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryVariant">@android:color/black</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@android:color/white</item>
        <item name="colorSecondaryVariant">@android:color/black</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- 액티비티 윈도우 배경색 -->
        <item name="android:windowBackground">@android:color/black</item>

        <!-- 기본 텍스트 색깔 -->
        <item name="android:textColor">@android:color/white</item>

        <!-- 폰트 적용 -->
        <item name="android:fontFamily">@font/app_font</item>
    </style>

    <style name="BaseButton" parent="android:style/Widget.Button">
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="RectangleButton" parent="BaseButton">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/button_rectangle_selector</item>
    </style>

    <style name="RoundRectangleButton" parent="BaseButton">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/button_round_rectangle_selector</item>
    </style>

    <style name="RoundRectangleButton18" parent="RoundRectangleButton">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="BorderButton" parent="BaseButton">
        <item name="android:textColor">@color/selected_text_color</item>
        <item name="android:background">@drawable/button_border_selector</item>
    </style>

    <style name="WhiteRadioButton" parent="android:style/Widget.CompoundButton.RadioButton">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:button">@drawable/radio_white_button_selector</item>
    </style>

</resources>